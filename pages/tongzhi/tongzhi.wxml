
<view class='main' wx:for="{{noticesinfo}}" data-code="{{item.id}}">
    <view class='title'>{{item.title}}</view>
    <view class='content'>{{item.infor}}</view>
    <view class='date'>{{item.datetime}}</view>
    <view class='line'></view>
</view>
